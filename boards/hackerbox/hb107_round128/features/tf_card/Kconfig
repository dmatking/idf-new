menu "TF Card (SD)"

    config TF_CARD_MOUNT_POINT
        string "Mount point"
        default "/sdcard"
        help
            VFS path where the SD card will be mounted.

    config TF_CARD_MAX_FILES
        int "Max open files"
        default 5
        range 1 20

endmenu
